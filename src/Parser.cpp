/*
    * Implements the regex parser.
    * Tokenizes the input regex pattern.
    * Builds the Abstract Syntax Tree (AST) representing the regex pattern.
    * Performs syntax analysis and verifies the correctness of the pattern.
*/