/*
    * Implements the matching logic using the DFA representation.
    * Provides functions to match input texts against the compiled regex pattern.
    * Handles capturing groups, backreferences, and other matching-related features.
*/